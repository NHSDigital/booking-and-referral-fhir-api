type: object
properties:
  Endpoint:
    description: This **MUST** be an **Endpoint** resource
    type: object
    properties:
      resourceType:
        type: string
      meta:
        type: object
        properties:
          profile:
            type: string
x-examples:
  example:
    resourceType: Endpoint
    meta: 
      profile: http://hl7.org/fhir/structuredefinition/endpoint
