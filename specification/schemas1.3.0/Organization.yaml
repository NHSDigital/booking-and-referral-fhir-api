type: object
properties:
  Endpoint:
    description: This **MUST** be an **Organization** resource
    type: object
    properties:
      resourceType:
        type: string
      meta:
        type: object
        properties:
          profile:
            type: string
x-examples:
  example:
    resourceType: Endpoint
    meta:
      profile: 'https://fhir.hl7.org.uk/StructureDefinition/UKCore-Organization'
